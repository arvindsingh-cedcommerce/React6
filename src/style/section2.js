import { Typography } from "@mui/material";
import styled from "styled-components";

export const Section2Head = styled(Typography)(() => ({
    color:'#2E2F2E',
    padding: '40px',
    paddingBottom: '10px',
}));