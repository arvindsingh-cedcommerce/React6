
import  styled  from 'styled-components';
import { Box } from '@mui/system';
import { Typography } from '@mui/material';

export const Section3Box = styled(Box)(() => ({
    backgroundColor:'#E5E5E5',
    padding:'3em'
}))
export const Section3Head = styled(Typography)(() => ({
    color:'white'
}))
export const Boxes = styled(Box)(() => ({
  backgroundColor:'#41DA63',
  width:'180px',
  height:'145px',
  alignItems: 'center',
  textAlign: 'center',
  justifyContent: 'center',
  paddingTop:'30px',
  margin:'0 25px 15px 0px'
}))